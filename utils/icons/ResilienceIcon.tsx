"use client";

import { ChakraProps, Icon } from "@chakra-ui/react";
import { GiStrong } from "react-icons/gi";

const ResilienceIcon = (props: ChakraProps) => {
  return <Icon as={GiStrong} {...props} />;
};

export default ResilienceIcon;
